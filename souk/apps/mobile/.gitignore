# Generated by `expo prebuild`. Regenerated on demand, so it is not tracked —
# but note that any manual edit inside these folders is lost on the next
# prebuild. Native customisation belongs in app.json or a config plugin.
/android
/ios
.expo
