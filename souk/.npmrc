# React Native's Metro bundler cannot follow pnpm's strict symlinked layout:
# transitive peer dependencies of expo-router and react-native resolve from
# the app directory and are simply not visible there. A hoisted node_modules
# is the accepted convention for React Native monorepos, and matches what
# npm and yarn do anyway.
node-linker=hoisted
